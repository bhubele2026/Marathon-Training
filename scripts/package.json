{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "generate-plan": "tsx ./src/generate-plan.ts",
    "seed": "tsx ./src/seed.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workspace/db": "workspace:*",
    "@workspace/plan-generator": "workspace:*",
    "drizzle-orm": "catalog:",
    "tsx": "catalog:"
  }
}
